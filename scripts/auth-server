#!/bin/bash

# TODO: Replace with nc

echo "Waiting for database"
#while ! /bin/nc -z mysql_for_auth 3306; do
  sleep 10
#done
echo "...database ready!"

java -jar \
  jetty-runner.jar \
  hspc-reference-authorization.war
